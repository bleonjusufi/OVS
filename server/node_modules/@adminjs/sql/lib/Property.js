import { BaseProperty } from 'adminjs';
export class Property extends BaseProperty {
    _isPrimary;
    _isNullable;
    _isEditable;
    _referencedTable;
    _name;
    constructor(column) {
        const { name, isId, position, isNullable, isEditable, type, referencedTable, } = column;
        super({
            path: name,
            isId,
            position,
            type,
        });
        this._name = name;
        this._isPrimary = isId;
        this._isNullable = isNullable;
        this._isEditable = isEditable;
        this._referencedTable = referencedTable;
    }
    isId() {
        return this._isPrimary;
    }
    name() {
        return this._name;
    }
    path() {
        return this._name;
    }
    isEditable() {
        return this._isEditable && !this.isId();
    }
    reference() {
        return this._referencedTable;
    }
    // eslint-disable-next-line class-methods-use-this
    availableValues() {
        // Currently "availableValues" have to be set explicitly via resource options
        return null;
    }
    // eslint-disable-next-line class-methods-use-this
    subProperties() {
        return [];
    }
    isRequired() {
        return !this._isNullable;
    }
}
//# sourceMappingURL=Property.js.map